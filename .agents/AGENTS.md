# Business Helper — Agent Guidelines

**Read [`CLAUDE.md`](../CLAUDE.md) at the repository root. It is the single operating authority
for all AI agents in this repo** — commands, architecture map, hard rules (production-first
targets, never-simulate, multi-tenant scoping, migration ordering), and the docs router.

This file previously carried its own rule set; it was consolidated into `CLAUDE.md` on 2026-08-07
so there is exactly one source of truth. Git history retains the old content.
