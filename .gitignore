# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/test-results/
/playwright-report/


# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env
.env.production

# Sentry source-map upload token, written here by `npx @sentry/wizard` and read
# by withSentryConfig at build time. Secret — it can write releases to the org.
.env.sentry-build-plugin

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# OS metadata
.DS_Store
Thumbs.db


# media & demo assets
/assets/

# Claude Code: shared rules live in .claude/settings.json (committed). This file is
# where "Yes, don't ask again" writes each person's own approvals — personal, and a
# guaranteed conflict if committed.
.claude/settings.local.json
